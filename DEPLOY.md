#!/bin/bash

# GigLedger MVP 部署脚本
# 需要用户手动创建 Cloudflare API Token

echo "=== GigLedger MVP 部署指南 ==="
echo ""
echo "问题：当前 Cloudflare API Token 权限不足"
echo ""
echo "解决方案："
echo "1. 登录 Cloudflare Dashboard: https://dash.cloudflare.com"
echo "2. 进入 API Tokens → Create Token"
echo "3. 选择 'Edit Cloudflare Workers' template"
echo "4. 配置权限："
echo "   - Zone: DNS (Edit)"
echo "   - Zone: Workers Routes (Edit)"
echo "   - Zone: Workers Scripts (Edit)"
echo "   - Zone: Workers Domains (Edit)"
echo ""
echo "5. Token 名称：gigledger-pages-deploy"
echo "6. 绑定到域名：bc417e42ff02039fe2a08cde3af3b983 (gigledger.xyz)"
echo ""
echo "创建 Token 后，运行："
echo "export CLOUDFLARE_API_TOKEN='你的新Token'"
echo "cd /root/.openclaw/workspace-main/gigledger"
echo "npx wrangler pages deploy dist"
echo ""
echo "或使用现有 Token 手动绑定："
echo "1. 访问 https://gigledger.xyz/"
echo "2. 检查 Workers 是否正常工作"
echo "3. 手动添加 Custom Domain: gigledger.xyz"
echo ""
echo "当前项目状态：✅ 代码完整，构建成功"
echo "构建产物：dist/ 目录 (392KB，7.10KB CSS)"